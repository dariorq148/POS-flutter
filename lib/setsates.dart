/*
void _incrementProduct(T producto) {
  setState(() {
    producto.cantidad++;
  });
}

void _decrementProduct(T producto) {
  setState(() {
    producto.cantidad--;
  });
}

// En el widget padre:
SelectionDisplay<Productos>(
selectedIndices: selectedPizzaIndices,
productos: widget.pizzas,
onIncrement: _incrementProduct,
onDecrement: _decrementProduct,
);
*/
